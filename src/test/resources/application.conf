signature-verifier {
  kafka {
    url = "localhost:6001"
    topic {
      incoming: ["incoming"]
      outgoing {
        valid: "valid"
        invalid: "invalid"
      }
    }
    stopTimeout = 1 ms
  }
}
