kafka {
  url = ${KAFKA_URL}
  topic {
    incoming: ${KAFKA_TOPIC_JSON_CANONICAL_PAYLOAD}
    outgoing {
      valid: ${KAFKA_TOPIC_JSON_CANONICAL_PAYLOAD_VALID}
      invalid: ${KAFKA_TOPIC_JSON_CANONICAL_PAYLOAD_INVALID}
    }
  }
}

akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "INFO"
  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"
}

ubirchKeyService.client.rest.host = "http://localhost:8095"
ubirchKeyService.client.rest.host = ${?UBIRCH_KEYSERVICE_URL}
